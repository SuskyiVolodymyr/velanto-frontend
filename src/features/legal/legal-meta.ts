// The date the Terms and Privacy documents were last changed. Kept
// locale-neutral (ISO 8601) so it reads the same in every locale, and shared
// by both pages so a copy update is a one-line change. Bump this whenever the
// legal copy in messages/*.json changes materially.
export const LEGAL_LAST_UPDATED = "2026-07-16";
